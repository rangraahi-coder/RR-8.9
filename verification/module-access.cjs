const fs=require('fs'),vm=require('vm'),assert=require('assert/strict'),ts=require('typescript'),path=require('path');const exportsObj={};vm.runInNewContext(ts.transpileModule(fs.readFileSync(path.join(__dirname,'../src/lib/moduleAccess.ts'),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{exports:exportsObj});
const {routeAllowed,hasPermission}=exportsObj;const worker={active:true,is_admin:false,permissions:{dispatch:['view','create']}};
assert.equal(routeAllowed(worker,'/dispatch'),true);assert.equal(routeAllowed(worker,'/contractor-finishing'),false);assert.equal(routeAllowed(worker,'/users'),false);assert.equal(routeAllowed(worker,'/masters/items'),false);assert.equal(hasPermission(worker,'dispatch','delete'),false);assert.equal(routeAllowed({...worker,active:false},'/dispatch'),false);assert.equal(routeAllowed(null,'/'),false);assert.equal(routeAllowed({...worker,is_admin:true,is_owner:true},'/users'),true);assert.equal(routeAllowed({...worker,is_admin:true,is_owner:true},'/contractor-finishing'),true);console.log('PASS: module routes, aliases, inactive users, action limits and unrestricted Owner');
