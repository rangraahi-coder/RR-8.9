const {PGlite}=require('@electric-sql/pglite');const fs=require('fs');const assert=require('assert');
(async()=>{const db=new PGlite();await db.exec(`CREATE ROLE anon; CREATE ROLE authenticated; CREATE SCHEMA auth; CREATE FUNCTION auth.uid() RETURNS uuid LANGUAGE sql AS $$SELECT null::uuid$$; CREATE FUNCTION public.erp_can(text,text) RETURNS boolean LANGUAGE sql AS $$SELECT true$$; CREATE SCHEMA storage; CREATE TABLE storage.buckets(id text PRIMARY KEY,name text,public boolean,file_size_limit bigint,allowed_mime_types text[]); CREATE TABLE storage.objects(bucket_id text,name text); CREATE FUNCTION storage.foldername(text) RETURNS text[] LANGUAGE sql AS $$SELECT string_to_array($1,'/')$$;CREATE TABLE stitch_operators(id serial,operator_code text UNIQUE,operator_name text); INSERT INTO stitch_operators(operator_code) VALUES('OP-06'),('OP-26');`);const sql=fs.readFileSync('02-OPERATOR-DOCUMENTS-AUTO-CODE.sql','utf8');await db.exec(sql);await db.exec(`INSERT INTO stitch_operators(operator_name) VALUES('A'),('B')`);assert.deepEqual((await db.query(`SELECT operator_code FROM stitch_operators ORDER BY id`)).rows.map(x=>x.operator_code),['OP-06','OP-26','OP-27','OP-28']);await db.exec(sql);await db.exec(`INSERT INTO stitch_operators(operator_name) VALUES('C')`);assert.equal((await db.query(`SELECT operator_code FROM stitch_operators WHERE operator_name='C'`)).rows[0].operator_code,'OP-29');console.log('PASS starts OP-27, preserves existing codes, unique consecutive allocations, rerun does not reset sequence');await db.close()})().catch(e=>{console.error(e);process.exit(1)});
