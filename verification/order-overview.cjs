const fs=require('fs'),ts=require('typescript'),assert=require('node:assert/strict');const m={exports:{}};new Function('module','exports',ts.transpileModule(fs.readFileSync('src/lib/orderOverview.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(m,m.exports);const calc=m.exports.orderOverview;
const o=[{id:'o',status:'pending'}],l=[{id:'l',sales_order_id:'o',item_name:'SET',qty:100}],s=[{id:'s',style_no:'SET'}],c=[{style_id:'s',component_name:'Kurta',qty_per_set:1},{style_id:'s',component_name:'Pant',qty_per_set:1},{style_id:'s',component_name:'Dupatta',qty_per_set:1}];let r=calc(o,l,s,c,[{order_item_id:'l',ready_qty:40}])[0];assert.equal(r.pending,60);assert.equal(r.components,300);assert.equal(r.pendingComponents,180);assert.equal(calc(o,l,s,c,[])[0].pending,null);assert.equal(calc([{id:'o',status:'cancelled'}],l,s,c,[]).length,0);assert.equal(calc(o,l,[...s,{id:'s2',style_no:'SET'}],c,[])[0].components,null);assert.equal(calc(o,l,s,c,[{order_item_id:'l',ready_qty:101}])[0].pending,null);console.log('PASS: 100/40/60, 300/180, unknown readiness, cancellation, ambiguous composition and stale readiness');
