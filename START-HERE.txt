CUTTING COMPONENT ROLL LINK FIX

Copy this complete src folder into RR-8.9 and choose Replace. Commit and Push, then wait for successful Vercel deployment. This change needs no NEW SQL; prior database updates remain required.

New cutting vouchers retain component, fabric and unit on every roll. Edit restores each roll only to its own component. View shows component roll counts/names. Component averages now use these separate rolls.

Old multi-component vouchers did not save ownership. On Edit, select the component for each saved roll, check the counts (e.g. Kurta 6 / Pant 4), and Continue to edit. Review fabric/unit and quantities, then Save to persist the mapping. Cancel leaves the stored voucher unchanged. Do not guess ownership from list order.

No live stock or historical voucher has been changed by this ZIP. If an old voucher was already saved with duplicated rolls, its quantities need reconciliation against the original entry; this update does not silently alter them.

Verified locally: 6+4 roll JSON roundtrip, reversed component order, distinct fabrics, total consumption unchanged, legacy assignment requirement, TypeScript check. Live deployment not tested.

GitHub Summary:
Fix cutting voucher component-wise roll restoration

Description:
Persist roll component/fabric/unit metadata, avoid copying all rolls into every component on edit, show component roll counts in view, and add explicit one-time assignment for legacy vouchers.
